# Example import files

| File | Purpose |
|------|---------|
| [legacy-v114-import.example.json](legacy-v114-import.example.json) | Legacy shape from official Default Google Account v1.14 (`rules` + `defaultAccount`). Replace emails and `accountId` values with yours. |

Import via BEDAG popup → **Settings → Import / Export → Import**.

Do not commit real exports containing your email addresses — use [.gitignore](../.gitignore) patterns.
